set(LLVM_TARGET_DEFINITIONS RockOps.td)

mlir_tablegen(RockOps.h.inc -gen-dialect-decls -I ${MLIR_INCLUDE_DIRS})
list(APPEND ORCHESTRA_INCGEN_HDRS
  "${CMAKE_CURRENT_BINARY_DIR}/RockOps.h.inc")

mlir_tablegen(RockOps.cpp.inc -gen-dialect-defs -I ${MLIR_INCLUDE_DIRS})
list(APPEND ORCHESTRA_INCGEN_SRCS
  "${CMAKE_CURRENT_BINARY_DIR}/RockOps.cpp.inc")

set(ORCHESTRA_INCGEN_HDRS ${ORCHESTRA_INCGEN_HDRS} PARENT_SCOPE)
set(ORCHESTRA_INCGEN_SRCS ${ORCHESTRA_INCGEN_SRCS} PARENT_SCOPE)
